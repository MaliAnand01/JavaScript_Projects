# .gitignore (in the QuoteGenerator root directory)

# Node.js dependencies
# --------------------
# Ignore the node_modules folder inside your netlify/functions directory
netlify/functions/node_modules/

# Optional: Ignore package-lock.json if you want to allow npm to resolve
# dependencies dynamically on deploy. However, for consistent builds,
# it's generally recommended to commit package-lock.json, so you might
# remove this line if you prefer that.
# netlify/functions/package-lock.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for compiled output
dist/
build/
.cache/

# Editor/OS specific files
.vscode/        # VS Code settings (optional, can be useful to share)
.idea/          # IntelliJ IDEA projects
.DS_Store       # macOS specific
Thumbs.db       # Windows specific
*.suo           # Visual Studio settings
*.user          # Visual Studio settings
*.bak           # Backup files
*.swp           # Vim swap files

# Environment variables
.env
.env.*